import React from 'react';

const Login = (props) => {
    return (
            <>
                <h1>Login</h1>
                <p>Only logged in users can see all pages</p>
            </>
        )
}

export default Login;